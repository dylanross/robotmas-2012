The StandardFirmata sketch must be loaded onto the arduino.

The following python modules are required :

	NumPy
	SciPy
	matplotlib
	pyFirmata (requires pySerial)
	PyBrain
	PIL

OpenCV must be installed, with python bindings.
